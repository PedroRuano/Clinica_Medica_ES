import React, { useEffect, useState } from "react";

// this menu will only be loaded when the user that logged in is a front desk member

export const FrontDeskMenu = () => {

    return(
        <div>  
        </div>
    )
    
}
