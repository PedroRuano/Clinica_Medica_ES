import React, { useEffect, useState } from "react";

// this menu will only be loaded when the user that logged in is a pacient

export const PacientMenu = () => {

    return(
        <div>  
        </div>
    )
    
}
