import React, { useEffect, useState } from "react";

// Page to request medical exams for the patient 

// can save the request data on a temp object until some one from the front desk checks it and accepts or rejects it
// front desk person also has to put in on the doctor/nurse callendar

export const RequestExams = () => {

    return(
        <div>  
        </div>
    )
    
}
