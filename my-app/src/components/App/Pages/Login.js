import React, { useEffect, useState } from "react";

// Login page that will have a error message with wrong password

export const Login = () => {

    return(
        <div>  
        </div>
    )
    
}
